// Gen ticker!
// 1. Is idle?
// 2. Request position in queue 
// 3. Create chunk buffer 
// 4. Generate chunk!

/// Max tasks at one time
const MAX_TASKS = 64;

// Main realisation
pub fn generator() {
    let pos = request_gen()?;

}

pub fn init() {
    meta("generator", MAX_TASKS)
}